import { SourceConfig } from '../../src/model/config'

export const mockConfig: SourceConfig = {
    serviceModeApiUrl: 'https://keeper.example.com',
    serviceModeApiKey: 'xxx123',
}

export const invalidConfig = {} as SourceConfig
